-- Generated by fpl_ingest.contract.compile_contract()
-- schema_version: 1.0.0

CREATE TABLE IF NOT EXISTS players (
    id INTEGER NOT NULL PRIMARY KEY,
    first_name TEXT NOT NULL,
    second_name TEXT NOT NULL,
    web_name TEXT NOT NULL,
    known_name TEXT,
    team INTEGER NOT NULL,
    team_code INTEGER NOT NULL,
    element_type INTEGER NOT NULL,
    now_cost INTEGER NOT NULL,
    price_change_percent INTEGER,
    status TEXT NOT NULL,
    code INTEGER NOT NULL,
    opta_code TEXT,
    photo TEXT,
    birth_date TEXT,
    team_join_date TEXT,
    region INTEGER,
    squad_number INTEGER,
    special INTEGER,
    removed INTEGER,
    can_transact INTEGER,
    can_select INTEGER,
    has_temporary_code INTEGER,
    total_points INTEGER NOT NULL,
    event_points INTEGER NOT NULL,
    minutes INTEGER NOT NULL,
    goals_scored INTEGER NOT NULL,
    assists INTEGER NOT NULL,
    clean_sheets INTEGER NOT NULL,
    goals_conceded INTEGER NOT NULL,
    own_goals INTEGER NOT NULL,
    penalties_saved INTEGER NOT NULL,
    penalties_missed INTEGER NOT NULL,
    yellow_cards INTEGER NOT NULL,
    red_cards INTEGER NOT NULL,
    saves INTEGER NOT NULL,
    bonus INTEGER NOT NULL,
    bps INTEGER NOT NULL,
    starts INTEGER NOT NULL,
    tackles INTEGER,
    recoveries INTEGER,
    clearances_blocks_interceptions INTEGER,
    defensive_contribution INTEGER,
    dreamteam_count INTEGER NOT NULL,
    in_dreamteam INTEGER NOT NULL,
    influence REAL NOT NULL,
    creativity REAL NOT NULL,
    threat REAL NOT NULL,
    ict_index REAL NOT NULL,
    expected_goals REAL NOT NULL,
    expected_assists REAL NOT NULL,
    expected_goal_involvements REAL NOT NULL,
    expected_goals_conceded REAL NOT NULL,
    clean_sheets_per_90 REAL,
    goals_conceded_per_90 REAL,
    saves_per_90 REAL,
    expected_goals_per_90 REAL,
    expected_assists_per_90 REAL,
    expected_goal_involvements_per_90 REAL,
    expected_goals_conceded_per_90 REAL,
    defensive_contribution_per_90 REAL,
    starts_per_90 REAL,
    form REAL NOT NULL,
    points_per_game REAL NOT NULL,
    selected_by_percent REAL NOT NULL,
    value_form REAL NOT NULL,
    value_season REAL NOT NULL,
    ep_next REAL,
    ep_this REAL,
    form_rank INTEGER NOT NULL,
    form_rank_type INTEGER NOT NULL,
    points_per_game_rank INTEGER NOT NULL,
    points_per_game_rank_type INTEGER NOT NULL,
    now_cost_rank INTEGER NOT NULL,
    now_cost_rank_type INTEGER NOT NULL,
    selected_rank INTEGER NOT NULL,
    selected_rank_type INTEGER NOT NULL,
    influence_rank INTEGER NOT NULL,
    influence_rank_type INTEGER NOT NULL,
    creativity_rank INTEGER NOT NULL,
    creativity_rank_type INTEGER NOT NULL,
    threat_rank INTEGER NOT NULL,
    threat_rank_type INTEGER NOT NULL,
    ict_index_rank INTEGER NOT NULL,
    ict_index_rank_type INTEGER NOT NULL,
    chance_of_playing_next_round INTEGER,
    chance_of_playing_this_round INTEGER,
    transfers_in INTEGER NOT NULL,
    transfers_out INTEGER NOT NULL,
    transfers_in_event INTEGER NOT NULL,
    transfers_out_event INTEGER NOT NULL,
    cost_change_event INTEGER NOT NULL,
    cost_change_event_fall INTEGER NOT NULL,
    cost_change_start INTEGER NOT NULL,
    cost_change_start_fall INTEGER NOT NULL,
    penalties_order INTEGER,
    penalties_text TEXT,
    corners_and_indirect_freekicks_order INTEGER,
    corners_and_indirect_freekicks_text TEXT,
    direct_freekicks_order INTEGER,
    direct_freekicks_text TEXT,
    news TEXT,
    news_added TEXT,
    ingested_at TEXT
);

CREATE TABLE IF NOT EXISTS teams (
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    short_name TEXT NOT NULL,
    code INTEGER NOT NULL,
    pulse_id INTEGER,
    strength INTEGER NOT NULL,
    strength_overall_home INTEGER NOT NULL,
    strength_overall_away INTEGER NOT NULL,
    strength_attack_home INTEGER NOT NULL,
    strength_attack_away INTEGER NOT NULL,
    strength_defence_home INTEGER NOT NULL,
    strength_defence_away INTEGER NOT NULL,
    played INTEGER NOT NULL,
    win INTEGER NOT NULL,
    draw INTEGER NOT NULL,
    loss INTEGER NOT NULL,
    points INTEGER NOT NULL,
    position INTEGER NOT NULL,
    form REAL,
    team_division TEXT,
    unavailable INTEGER NOT NULL,
    ingested_at TEXT
);

CREATE TABLE IF NOT EXISTS fixtures (
    id INTEGER NOT NULL PRIMARY KEY,
    code INTEGER NOT NULL,
    event INTEGER,
    team_h INTEGER NOT NULL,
    team_a INTEGER NOT NULL,
    team_h_score INTEGER,
    team_a_score INTEGER,
    team_h_difficulty INTEGER NOT NULL,
    team_a_difficulty INTEGER NOT NULL,
    kickoff_time TEXT,
    minutes INTEGER NOT NULL,
    started INTEGER,
    finished INTEGER NOT NULL,
    finished_provisional INTEGER NOT NULL,
    provisional_start_time INTEGER,
    pulse_id INTEGER,
    ingested_at TEXT
);
CREATE INDEX IF NOT EXISTS idx_fixtures_event ON fixtures(event);

CREATE TABLE IF NOT EXISTS fixture_stats (
    fixture_id INTEGER NOT NULL,
    identifier TEXT NOT NULL,
    element INTEGER NOT NULL,
    value INTEGER NOT NULL,
    side TEXT NOT NULL,
    ingested_at TEXT,
    UNIQUE(fixture_id, identifier, element)
);
CREATE INDEX IF NOT EXISTS idx_fixture_stats_element ON fixture_stats(element);

CREATE TABLE IF NOT EXISTS gameweeks (
    element_id INTEGER NOT NULL,
    round INTEGER NOT NULL,
    minutes INTEGER NOT NULL,
    goals_scored INTEGER NOT NULL,
    assists INTEGER NOT NULL,
    clean_sheets INTEGER NOT NULL,
    goals_conceded INTEGER NOT NULL,
    own_goals INTEGER NOT NULL,
    penalties_saved INTEGER NOT NULL,
    penalties_missed INTEGER NOT NULL,
    yellow_cards INTEGER NOT NULL,
    red_cards INTEGER NOT NULL,
    saves INTEGER NOT NULL,
    bonus INTEGER NOT NULL,
    bps INTEGER NOT NULL,
    total_points INTEGER NOT NULL,
    influence REAL NOT NULL,
    creativity REAL NOT NULL,
    threat REAL NOT NULL,
    ict_index REAL NOT NULL,
    expected_goals REAL NOT NULL,
    expected_assists REAL NOT NULL,
    expected_goal_involvements REAL NOT NULL,
    expected_goals_conceded REAL NOT NULL,
    starts INTEGER NOT NULL,
    in_dreamteam INTEGER NOT NULL,
    tackles INTEGER,
    clearances_blocks_interceptions INTEGER,
    recoveries INTEGER,
    defensive_contribution INTEGER,
    ingested_at TEXT,
    UNIQUE(element_id, round)
);
CREATE INDEX IF NOT EXISTS idx_gameweeks_round ON gameweeks(round);

CREATE TABLE IF NOT EXISTS player_histories (
    element_id INTEGER NOT NULL,
    round INTEGER NOT NULL,
    fixture INTEGER NOT NULL,
    minutes INTEGER NOT NULL,
    goals_scored INTEGER NOT NULL,
    assists INTEGER NOT NULL,
    clean_sheets INTEGER NOT NULL,
    goals_conceded INTEGER NOT NULL,
    own_goals INTEGER NOT NULL,
    penalties_saved INTEGER NOT NULL,
    penalties_missed INTEGER NOT NULL,
    yellow_cards INTEGER NOT NULL,
    red_cards INTEGER NOT NULL,
    saves INTEGER NOT NULL,
    bonus INTEGER NOT NULL,
    bps INTEGER NOT NULL,
    total_points INTEGER NOT NULL,
    influence REAL NOT NULL,
    creativity REAL NOT NULL,
    threat REAL NOT NULL,
    ict_index REAL NOT NULL,
    expected_goals REAL NOT NULL,
    expected_assists REAL NOT NULL,
    expected_goal_involvements REAL NOT NULL,
    expected_goals_conceded REAL NOT NULL,
    starts INTEGER NOT NULL,
    in_dreamteam INTEGER NOT NULL,
    tackles INTEGER,
    clearances_blocks_interceptions INTEGER,
    recoveries INTEGER,
    defensive_contribution INTEGER,
    opponent_team INTEGER NOT NULL,
    was_home INTEGER NOT NULL,
    kickoff_time TEXT NOT NULL,
    team_h_score INTEGER,
    team_a_score INTEGER,
    value INTEGER NOT NULL,
    selected INTEGER NOT NULL,
    transfers_in INTEGER NOT NULL,
    transfers_out INTEGER NOT NULL,
    transfers_balance INTEGER NOT NULL,
    ingested_at TEXT,
    UNIQUE(element_id, round, fixture)
);
CREATE INDEX IF NOT EXISTS idx_player_histories_round ON player_histories(round);
CREATE INDEX IF NOT EXISTS idx_player_histories_element_id ON player_histories(element_id);

CREATE TABLE IF NOT EXISTS events (
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    deadline_time TEXT NOT NULL,
    deadline_time_epoch INTEGER NOT NULL,
    deadline_time_game_offset INTEGER NOT NULL,
    release_time TEXT,
    released INTEGER,
    average_entry_score INTEGER,
    highest_score INTEGER,
    highest_scoring_entry INTEGER,
    ranked_count INTEGER,
    finished INTEGER NOT NULL,
    data_checked INTEGER NOT NULL,
    is_previous INTEGER NOT NULL,
    is_current INTEGER NOT NULL,
    is_next INTEGER NOT NULL,
    can_enter INTEGER NOT NULL,
    can_manage INTEGER NOT NULL,
    cup_leagues_created INTEGER NOT NULL,
    h2h_ko_matches_created INTEGER NOT NULL,
    most_selected INTEGER,
    most_transferred_in INTEGER,
    most_captained INTEGER,
    most_vice_captained INTEGER,
    top_element INTEGER,
    top_element_points INTEGER,
    transfers_made INTEGER NOT NULL,
    chip_plays_json TEXT,
    ingested_at TEXT
);

CREATE TABLE IF NOT EXISTS element_types (
    id INTEGER NOT NULL PRIMARY KEY,
    singular_name TEXT NOT NULL,
    singular_name_short TEXT NOT NULL,
    plural_name TEXT NOT NULL,
    plural_name_short TEXT NOT NULL,
    squad_select INTEGER NOT NULL,
    squad_min_select INTEGER,
    squad_max_select INTEGER,
    squad_min_play INTEGER NOT NULL,
    squad_max_play INTEGER NOT NULL,
    ui_shirt_specific INTEGER NOT NULL,
    element_count INTEGER NOT NULL,
    ingested_at TEXT
);
